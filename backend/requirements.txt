asyncpg>=0.30.0
bcrypt>=4.0.0
fastapi>=0.115.0
httpx>=0.26.0
pydantic[email]>=2.10.0
pydantic-settings>=2.5.0
PyJWT>=2.9.0
pypdf>=5.0.0
python-docx>=1.1.0
python-dotenv>=1.0.0
python-multipart>=0.0.12
pywebpush>=2.0.0
uvicorn[standard]>=0.30.0
websockets>=12.0
reportlab>=4.0.0

# Planilha do relatório de vendedores (app/relatorios/vendedores.py).
openpyxl==3.1.5
# O relatório lê o HSGrowth com psycopg síncrono — é a conexão do script
# original do Erick, copiado fiel. O resto do backend usa asyncpg.
psycopg[binary]==3.2.3

# O agente escreve markdown na base de conhecimento e o editor da tela
# (TipTap) guarda HTML — a conversão acontece em app/routers/mcp_wiki.py.
markdown==3.7
